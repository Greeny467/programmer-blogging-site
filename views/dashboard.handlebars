{{> header}}
<main class="container container-fluid mt-5"></main>
    {{#if loggedIn}}
    <div>
        <a href="{{url}}/create" class="btn">Create new Post</a>
    </div>
    <div>
        {{#if arePosts}}
        {{#each posts as |post|}}
        <h3>{{post.title}}</h3>
        <p>{{post.text}}</p>
        <p>{{post.date}}</p>
        <a href="{{url}}/edit/{{post.id}}">edit post</a>

        {{/each}}
        {{/if}}
    </div>
    {{/if}}
</main>
<footer>
    
</footer>

<script>const siteUrl = {{url}}</script>
{{#if loggedIn}}
<script src="/js/logout.js"></script>
{{/if}}